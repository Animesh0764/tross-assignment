# Session cookies copied from a logged-in linkedin.com browser session (DevTools > Application > Cookies)
LI_AT=
JSESSIONID=

# Optional: require X-API-Key on /profile so the deployed instance is not open to the world
API_KEY=

# Optional: seconds to cache a profile in memory (default 3600)
CACHE_TTL=3600
